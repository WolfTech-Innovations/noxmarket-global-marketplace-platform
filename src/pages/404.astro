---
import Layout from '@/layouts/Layout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
---

<Layout title="Lost in the Matrix - Nox">
  <Header />

  <main class="py-12 min-h-screen flex items-center">
    <div class="container-custom w-full">
      <div class="max-w-3xl mx-auto">
        <div class="card-elevated p-12 text-center">
          <!-- Animated 404 -->
          <div class="mb-8 relative">
            <h1 class="text-9xl font-bold bg-gradient-to-br from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent animate-pulse">
              404
            </h1>
            <div class="absolute inset-0 flex items-center justify-center">
              <div class="w-32 h-32 bg-blue-500/10 rounded-full blur-3xl animate-ping"></div>
            </div>
          </div>

          <!-- Friendly Message -->
          <div class="space-y-4 mb-8">
            <h2 class="text-4xl font-medium text-gray-900">
              Oops! We've hit a dead end 🤔
            </h2>
            <p class="text-xl text-gray-600 max-w-xl mx-auto leading-relaxed">
              Looks like this component got lost in shipping. Even our best warehouse crew can't find this page!
            </p>
            <p class="text-gray-500">
              Maybe it was overclocked too hard, or perhaps it never existed in the first place...
            </p>
          </div>

          <!-- Quick Links Card -->
          <div class="bg-gradient-to-br from-blue-50 to-purple-50 rounded-2xl p-6 mb-8">
            <p class="text-sm font-semibold text-gray-700 mb-4">
              🔧 Let's get you back on track:
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
              <a href="/" class="card state-layer p-4 hover:scale-105 transition-transform">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-blue-100 rounded-xl flex items-center justify-center">
                    <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                    </svg>
                  </div>
                  <div class="text-left">
                    <p class="font-semibold text-gray-900">Home</p>
                    <p class="text-xs text-gray-600">Start fresh</p>
                  </div>
                </div>
              </a>

              <a href="/products" class="card state-layer p-4 hover:scale-105 transition-transform">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-purple-100 rounded-xl flex items-center justify-center">
                    <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                    </svg>
                  </div>
                  <div class="text-left">
                    <p class="font-semibold text-gray-900">Browse Components</p>
                    <p class="text-xs text-gray-600">Find your parts</p>
                  </div>
                </div>
              </a>
            </div>
          </div>

          <!-- Main CTA -->
          <div class="flex flex-col sm:flex-row gap-3 justify-center">
            <a href="/" class="btn btn-primary text-lg px-8">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
              </svg>
              Take Me Home
            </a>
            <a href="/products" class="btn btn-secondary text-lg px-8">
              Shop Components
              <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
          </div>

          <!-- Fun Tech Humor -->
          <div class="mt-8 pt-8 border-t border-gray-200">
            <p class="text-sm text-gray-500 italic">
              "Have you tried turning it off and on again?" - Classic IT advice that won't help here 😅
            </p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }
  
  h1 {
    animation: float 3s ease-in-out infinite;
  }
</style>